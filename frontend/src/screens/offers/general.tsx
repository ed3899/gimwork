import React, {useState} from "react";
import {View, Text} from "react-native";

const GeneralOffers: React.FC<any> = ({navigation}) => {
  return (
    <View>
      <Text>GeneralOffers</Text>
    </View>
  );
};

export default GeneralOffers;
